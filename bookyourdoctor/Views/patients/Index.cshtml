@model IEnumerable<bookyourdoctor.patient>
@{
    ViewBag.Title = "Index";
}
<head>
    <style type="text/css">

        body {
            background-image: url(../images/qqq.jpg);
            background-repeat: no-repeat;
            background-size: cover;
            height: 1700px;
        }

        .registration {
            color: black;
            font-size: 40px;
            font-family: Agency FB;
            font-weight: 700;
            border-bottom-style: ridge;
        }

        .row {
        }

        .btn.hover {
            background-color: aqua;
        }

        label {
            font-size: 22px;
        }

        .btn-danger {
            border-radius: 0px;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="row">
            <div class="col-md-8 col-md-offset-2" id=" form" style=" background-color:#acbdca;height:10% 100%;opacity:0.9;

    ">
                <center style="text-align:center ;
	padding-top: 60px;padding-bottom:60px"><b class="registration">PATIENTS</b></center>
                <table class="table">
                    <tr>
                        <th>
                            @Html.DisplayNameFor(model => model.name)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.email)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.contact_number)
                        </th>
                      
                        <th></th>
                    </tr>
                    @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.name)
                </td>
                c
                <td>
                    @Html.DisplayFor(modelItem => item.email)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.contact_number)
                </td>
                
                <td>
                    @Html.ActionLink("Edit", "Edit", "patients", new { ide = item.email }, null) |
                    @Html.ActionLink("Details", "Details", "patients", new { ide = item.email }, null) |
                    @Html.ActionLink("Delete", "Delete", "patients", new { ide = item.email }, null)
                </td>
            </tr>
}
                </table>


            </div>
        </div>
    </div>
</body>



